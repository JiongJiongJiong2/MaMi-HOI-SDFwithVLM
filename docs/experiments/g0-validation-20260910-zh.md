# G0 真实接触候选区域验证（2026-09-10）

## 当日追加：限定宽区域对照已完成，当前前置验证收口

已执行下文原“下一步”：相同 2330 个方向、63 个序列、512 候选与 0.08 空间分散距离。事先指定主方法为水平/竖直带的并集，带半宽固定 0.08 max_extent，保留原 inward penalty/reward，不使用法向或目标点调整分数；水平带、竖直带仅为预先列出的次要对照。没有参数搜索。

主方法 Top-5 **19.38%**，低于分散中心对跖点 **22.24%**，也低于此前法向截面方法 **26.56%**。相对中心对跖点差值 **-2.86 个百分点**，序列 bootstrap 95% 区间 **[-11.49, +5.72]**。次要水平带 Top-5/10 为 23.88%/38.67%，竖直带为 23.00%/32.65%；不能事后挑一个指标宣布主方法通过。

结论：**当前固定几何规则 NO-GO；不追加宽度/权重扫描，不运行 held-out test，不据此进入预测模型训练或接触优化。** 这是已测试规则的失败，不是否定所有几何先验。新区域预测模型或新目标属于下一次明确的实验设计，不再算“只差一个前置小检查”。

脚本 `scripts/audit_sectional_bands.py`，输出 `outputs/g0_bands_20260910_v1`。本地 CPU 14.43 秒，约 89.63 MiB；绑定输入未变。原先的服务器结果保留不动。本轮没有触发 GPU 工作。

SDF A 独立收口：BPS 参考与 cache replay 已通过；两容器材料正例/边界仍未建立，旧 penetration evaluator 未替换。当前输入下已测试的低成本 A 路径 NO-GO，原 signed U1 不具备训练条件。只有补充材料边界依据或明确改变实验几何/监督定义才可重新开启；不是继续重复同一批检查。

**执行通过，当前效果未达标。** 本轮优先推进接触候选区域预测，未训练模型、未执行接触优化、未启动 CUDA。原 G0 在真实 validation 上 FAIL；一次固定空间分散对照提高了覆盖，但尚无可靠优于同等处理基线的证据。不进入 held-out test 或接触优化。

## 执行范围

本地/AutoDL 起始提交均为 `5ab0bf5ac0eeb117aea5ea6c41fd8636c8a18f9b`，工作区干净，不需要拉取旧代码。服务器显示 RTX 4090 D、memory.max=66571993088，已有 NumPy 1.26.4、joblib 1.6.0、trimesh 5.1.0；没有使用 GPU 计算。

原脚本在 AutoDL 跑完冻结的 100 个 validation 序列，seed=1、512 个面积采样候选、frame_stride=5，其余命中/排除/法向参数保持不变。只把样本上限从 2000 提高到 1000000，以免按排序截断为少数前缀序列，最终实际样本远低于该上限。不是增加候选数或调整命中阈值。

冻结 split SHA-256 为 `665aeea1d076ec6ddf6605018511b4ee46f1ef9dd84ed4c09922a5d0187fe0db`。共享 physical test-window 文件按已有划分读取，只评估 validation 名单；没有评价 held-out test 序列或用其指标选规则。

625 个窗口经重叠帧去重后检查 3483 帧，其中 2247 帧不满足双手接触条件，71 帧被既有表面距离阈值拒绝，留下 **1165 帧、2330 个左右配对方向、63 个有效序列、11 个对象**。其余 37 个 validation 序列没有贡献最终有效样本；不能说结果覆盖了全部 100 个序列的有效接触。配对和 validation 成员关系复核通过。

## 原始 G0 结果

这里的区域命中定义为：Top-K 中至少一个候选距投影后的目标表面候选不超过物体最大尺度的 0.08。不是物理接触准确率，也不是手指抓取成功率。源手和目标来自 GT 手掌 proxy，再投影到同一组候选；当前未验证模型预测源手位置的噪声。

| 方法 | 原始 Top-1 命中 | 原始 Top-5 命中 | 原始 Top-10 命中 |
|---|---:|---:|---:|
| 随机 | 1.73% | 8.25% | 14.64% |
| 中心对跖点 | 10.89% | 16.72% | 19.96% |
| 截面弦 | 7.21% | 14.56% | 18.80% |
| 截面弦 + 法向 | 7.68% | 17.95% | 24.80% |

均先按序列平均，再进行序列宏平均。最好截面方法 Top-5 相对中心对跖点只增 **1.23 个百分点**，未达到预先定义的 5 个百分点。目标到弦的归一化中位距离 **0.2606**，也高于预设上限 **0.12**，因此两项原门槛均未满足。

## 固定空间分散对照

在看到原始失败后，只增加一项明确记录的 exploratory validation 对照：不改候选池、不改排序分数；按原排名顺序选择候选，要求相互距离至少为现有命中半径 `0.08 × max_extent`。四种方法全部采用相同处理，保留 Top-1，比较 Top-5/10。没有搜索半径或拟合新权重，所有样本都选满 10 个不同区域。

新脚本 `audit_sectional_region_coverage.py` 在本地复算服务器 CSV。原 PLY 声明坐标为 float，需复现 trimesh 的 float32 读取精度；首个 v1 因精度对照断言失败而停止，未产出有效指标。修正读取精度后保存新 v2，2330 样本的原始排序距离误差重放最大差异为 **0.0**，再进行对照。未修改 mesh 文件。

| 方法 | 空间分散 Top-5 | 空间分散 Top-10 |
|---|---:|---:|
| 随机 | 9.38% | 18.03% |
| 中心对跖点 | 22.24% | 27.30% |
| 截面弦 | 23.20% | 29.30% |
| 截面弦 + 法向 | 26.56% | 34.43% |

分散后的法向截面方法比同样分散的中心对跖点高 **4.32 个百分点**。以序列为单位、seed=1、10000 次配对 bootstrap 的 95% 区间为 **[-4.31, +12.87] 个百分点**，仍跨过零。该区间是探索性比较，不是独立测试结果。原始 FAIL 不被覆盖，空间分散也不改变目标离弦距离这一失败事实。

## 对象差异与诊断

下表为同一空间分散规则下的 Top-5 序列宏命中，不能根据本表再逐对象选择赢家并当成无偏总结果。

| 对象 | 序列 / 方向样本 | 中心对跖点 | 截面弦 | 截面弦 + 法向 |
|---|---:|---:|---:|---:|
| clothesstand | 4 / 98 | 76.56% | 78.27% | 90.38% |
| largebox | 10 / 432 | 1.15% | 14.71% | 18.01% |
| largetable | 7 / 270 | 0.00% | 5.78% | 10.89% |
| monitor | 4 / 122 | 0.00% | 51.66% | 49.68% |
| plasticbox | 8 / 220 | 0.00% | 17.17% | 16.44% |
| smallbox | 5 / 290 | 71.92% | 39.17% | 54.62% |
| smalltable | 6 / 246 | 0.00% | 8.89% | 5.25% |
| suitcase | 9 / 456 | 63.04% | 14.29% | 21.41% |
| trashcan | 6 / 64 | 0.00% | 2.50% | 2.50% |
| tripod | 2 / 48 | 78.12% | 98.44% | 98.44% |
| woodchair | 2 / 84 | 0.00% | 13.64% | 7.92% |

2330 个样本中只有 22 个目标最近候选落入源手排除区域；这一项不能解释总体低覆盖。以同样 0.08 归一化距离检查几何，目标处于竖直平面邻域的样本比例为 40.21%，水平平面邻域为 47.21%，弦邻域仅为 13.86%。这是样本级覆盖描述，不是四种排序的序列宏命中，也不证明平面带新方法已经有效。

证据支持两个待验证解释：Top-K 聚集确实限制覆盖；把另一只手强约束到两平面交弦可能过窄。候选投影误差、脏 mesh 法向以及 GT palm/contact proxy 也仍是限制，不能把失败都归于某一个机制。

## 决策与下一步

当前状态：**G0 RUN PASS / 原规则 EFFICACY FAIL；空间分散版本探索性改善，尚未建立可靠优势；held-out test NOT RUN。** U1 的 source/evaluation geometry blocker 保持独立，不因有 GPU 或 G0 执行完成而解除。

下一步只推进 G0 区域预测：预先固定一个“较宽水平/竖直截面区域 + 空间分散候选”的无训练版本，在相同 validation 样本和候选预算上比较；不要逐对象挑赢家或降低原始门槛后宣布原方法通过。区域规则稳定后，再进行预测源手位置扰动验收与一次冻结 held-out test。当前不进入接触优化、diffusion、U2/U3/U4 或大规模新网络。

## 证据与执行记录

服务器原始结果：`/root/autodl-tmp/mamihoi/outputs/sectional_prior/g0_validation_20260910_v1`。
本地镜像：`outputs/g0_validation_autodl_20260910_v1`。
空间分散复算：`outputs/g0_region_coverage_20260910_v2`。
逐对象 CSV / 汇总图：`outputs/g0_validation_review_20260910_v1`。

服务器 8 项原测试 PASS；主实验 57.07 秒，峰值子进程 RSS 486.51 MiB；117 个输入 hash 前后不变，服务器 Git 工作区保持干净。新增空间分散单元测试 2 项 PASS；本地复算 58.98 秒、峰值 working set 78.27 MiB，绑定输入未变。服务器原运行期间和复算均设置 CUDA_VISIBLE_DEVICES 为空。

本轮没有改训练、loss、dataset、evaluator、原 G0 排序或现有 SDF/cache。新增对照脚本和报告仍为本地未提交改动，未同步新脚本到服务器；已完成的服务器实验使用原有同版本代码。
